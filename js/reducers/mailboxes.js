import ActionTypes from '../actions/actionTypes/ethereumActionTypes';

const mailboxes = (
  state = [],
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mailboxes;
